# Raspberry Pi 3B+ voice client — openWakeWord (alexa) + mpv
#
# Do NOT list openwakeword / onnxruntime here: openwakeword hard-depends on
# onnxruntime, which has no linux/armv7 wheels. scripts/install-oww-runtime.sh
# installs openwakeword (--no-deps on armv7) + tflite-runtime or onnxruntime.
httpx>=0.27.0
numpy>=1.24.0,<2.0.0
sounddevice>=0.5.0
scipy>=1.11.0,<1.14.0
pymicro-wakeword>=2.4.1
